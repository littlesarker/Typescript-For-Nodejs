/*
 typescript  will give error if type not match
(Both type will give error if type does not match)
*/
// Expilicit
var ex = "Explicit Variable";
ex = 33;
//impilicit 
var im = "implicit variable";
im = false;
console.log(ex);
console.log(im);
